/*
* parser/aConstraint.js
*/

/*
* Return a set of 'writable's that can drive an arc.
*/
function aConstraint(start, end) {

  // tbd. How to make '.r' and '.center' drive it?
  //    + '.mid'

  return {
    start,
    end
  }
}
